// Generated by CoffeeScript 1.10.0
(function(window, $) {
  var HOUR, MINUTE, formatTimeDiff, getTd;
  HOUR = 60 * 60;
  MINUTE = 60;
  formatTimeDiff = function(td) {
    var hours, minutes, seconds;
    hours = Math.floor(td / HOUR);
    td = td - hours * HOUR;
    minutes = Math.floor(td / MINUTE);
    td = td - minutes * MINUTE;
    seconds = td;
    return hours + ":" + ($.zeroPad(minutes)) + ":" + ($.zeroPad(seconds));
  };
  getTd = function() {
    return Math.round((new Date()).getTime() / 1000);
  };
  $.fn.toCountdown = function() {
    var el, formattedDiff, now, targetTime;
    el = $(this);
    targetTime = el.data('timestamp');
    now = getTd();
    formattedDiff = formatTimeDiff(targetTime - now);
    return el.text($.template('countdown', {
      time: formattedDiff
    }));
  };
  return setInterval(function() {
    return ($('#filecast-time')).toCountdown();
  }, 1000);
})(this, this.jQuery);
