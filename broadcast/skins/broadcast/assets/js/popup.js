// Generated by CoffeeScript 1.10.0
(function(window, $) {
  var POPUP_TIMEOUT, body;
  body = $('body');
  POPUP_TIMEOUT = 7000;
  body.on('click', '.popup-close', function(e) {
    var el;
    el = $(this);
    return (el.parents('.popup')).remove();
  });
  return $.popup = function(message, timeout) {
    var popup;
    if (timeout == null) {
      timeout = POPUP_TIMEOUT;
    }
    ($('.popup')).remove();
    popup = $($.template('popup', {
      message: message
    }));
    body.append(popup);
    setTimeout(function() {
      return popup.remove();
    }, timeout);
    return popup;
  };
})(this, this.jQuery);
